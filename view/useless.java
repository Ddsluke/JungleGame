package view;

/*import board.Area;

import javax.swing.*;
import java.awt.*;

public class useless {
    bottom = new GameBoard();
    con = getContentPane();
    //b1.addActionListener(new newevent()); //start a new game
    //b2.addActionListener(new quitevent());
    //b3.addActionListener(new continueevent);
        p1.setLayout(new GridLayout(12,0));
        p1.add(b1);
        p1.add(b2);
        p1.add(b3);
        p2.setLayout(new GridLayout(7,9));


        for(int i=0;i<7;i++)
    {
        for(int j=0;j<9;j++)
        {
            if(bottom.pieceBoard[i][j]==null) {
                button[i][j] = new JButton();
            }
            else
            {

                String name=bottom.pieceBoard[i][j].name;

                button[i][j]=new JButton();

                piece[i][j]=new RoundButton(name);


                if(bottom.pieceBoard[i][j].side==0) {
                    piece[i][j].setBackground(Color.RED);

                }

                else
                {
                    piece[i][j].setForeground(Color.YELLOW);

                }
                button[i][j].add(piece[i][j]);

            }
            if(bottom.board[i][j]!=null)
            {
                if (bottom.board[i][j] == Area.RIVER)
                    button[i][j].setBackground(Color.green);
                if (bottom.board[i][j] == Area.TRAP)
                    button[i][j].setBackground(Color.black);
                if (bottom.board[i][j]==Area.DEN)
                    button[i][j].setBackground(Color.cyan);
            }
            else
                button[i][j].setBackground(Color.white);
            if(piece[i][j]!=null)
                p2.add(piece[i][j]);

        }
    }
}*/
