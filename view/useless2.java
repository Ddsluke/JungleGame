package view;
/*import javax.swing.*;
import java.awt.*;

import Piece.Piece;
import board.*;
import board.Area;
import javafx.scene.Group;
import javafx.scene.shape.Circle;

import java.awt.event.ActionEvent;
import java.awt.event.ActionListener;
import java.awt.geom.*;



public class junglegui extends JFrame {

    GameBoard bottom;
    Container con = null;

    Button b1 = new Button("NEW GAME");
    Button b2 = new Button("SAVE AND QUIT");
    Button b3 = new Button("CONTINUE");
    JPanel p1=new JPanel();
    JPanel p2=new JPanel();
    JPanel p3=new JPanel();
    private JButton button[][]=new JButton [7][9];
    private JButton piece[][]=new JButton[7][9];





    public junglegui() {

        bottom =new GameBoard();
        con = getContentPane();
        p1.add(b1);
        p1.add(b2);
        p1.add(b3);
        JSplitPane split = new JSplitPane(JSplitPane.HORIZONTAL_SPLIT, bottom, null);

        split.setRightComponent  (p1);
       //split.setLeftComponent(p2);
        split.setDividerSize(0); 
        split.setDividerLocation(760);
        con.add(split, BorderLayout.CENTER);
        setDefaultCloseOperation(EXIT_ON_CLOSE);

        setVisible(true);  
        int width = Toolkit.getDefaultToolkit().getScreenSize().width;
        int height = Toolkit.getDefaultToolkit().getScreenSize().height;
        setBounds((width-1100)/2, (height-800)/2, 900,  700);
        setTitle("JUNGLE GAME");
        con.validate();

        validate();

    }
    public static void main(String args[]) {
        new junglegui();
    }
}*/

























